---
version: v0.0.8
codename: hero-popover
status: in-progress
target_date: 2026-05-12
shipped_date: null
includes_specs:
  - 2026-05-11-hero-popover
release_notes_zh: |
  v0.0.8 — Hero popover

  Popover 视觉重做，参考 SessionWatcher 的视觉层级：
  - 5h 窗口：大字号 hero 卡片（56pt rounded semibold 数字 + 紧凑 reset countdown）
  - 7d 窗口：secondary 卡片（28pt 数字）
  - 进度条改用 capsule 形状，高度 8pt
  - reset 时间从 SwiftUI 默认 "in 1 hour" 换为紧凑 "1h 23m" / "12m" / "<1m"
  - popover 宽度 340 → 360pt
  - Per-Model（Opus/Sonnet）/ Extra Usage / 历史图表 / 控制行均保留不变
  - 无新依赖、不改 OAuth / 数据层；仅 SwiftUI 视图层重做
---

# v0.0.8 — hero-popover

## 主题

参考 SessionWatcher 的视觉风格重做 `PopoverView`：5h 窗口提升为大字号 hero 卡片，7d 窗口降级为 secondary 卡片，进度条改为 capsule 形状，reset countdown 用紧凑格式（"1h 23m" / "12m"）替换 SwiftUI 默认 relative。

不动数据层、不动 OAuth/Setup 流程、不动菜单栏图标（菜单栏多模式留给 v0.0.10）。

## 计划含的 spec

- [`2026-05-11-hero-popover`](../superpowers/specs/2026-05-11-hero-popover.md) — Popover 重做

## 调研依据

[`../research/competitive-analysis.md`](../research/competitive-analysis.md) §1.3 / §5.2 Step A

## 验收（G6 checklist）

- [x] spec 的 12 条 spec_criteria（SC1~SC12）全 done（spec `## Verification log` 全 [x]）
- [x] `swift build -c release` 绿（spec SC9 evidence）
- [x] `swift test` 全部 0 failures（49 tests，含 6 个 ResetCountdownFormatterTests）
- [ ] 启动 .app 目视确认 hero 字号、进度条 capsule 样式、配色阈值切换 — **由用户在菜单栏点开 popover 验收**（Sign in 后才进入 usageView 看到 hero 卡片；亦可在 Xcode 看 UsageHeroCard.swift 的 #Preview）
- [x] 本文件 `release_notes_zh` 已填
- [x] CHANGELOG.md 已 append v0.0.8 entry

## 发版（G7 checklist —— 在所有 v0.0.x 完成后统一决策）

参考 v0.0.7 决策：纯文档版本采用"只 push 不打 tag"；v0.0.8 含代码改动，但仍可与后续 v0.0.x 累积合并发版（minor 版 v0.1.0 时统一打 tag）。具体打 tag 时机由用户在 v0.1.0 里程碑时决定。

## 引用

- 路线：[`README.md`](./README.md)
- 母法：[`../superpowers/specs/2026-05-11-docs-governance.md`](../superpowers/specs/2026-05-11-docs-governance.md)
