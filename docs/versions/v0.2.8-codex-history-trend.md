---
version: v0.2.8
codename: codex-history-trend
status: in-progress
target_date: 2026-05-13
shipped_date: null
includes_specs: [2026-05-12-codex-history-trend]
release_notes_zh: |
  ### 改进
  - Codex 标签页新增**额度折线图**与 Session / Weekly 卡的**趋势箭头**（▲▼ + 增量百分点），与 Claude 标签页一致。
  - Codex 现在每 5 分钟在后台静默拉取一次额度，使折线图/趋势能持续积累数据（之前只在打开菜单时拉一次）。
  ### 内部
  - `UsageHistoryService` 泛化为可指定文件名/目录（Claude 历史路径行为不变；Codex 用独立的 `history-codex.json`）。
  - `UsageChartSectionView` 折线图区块参数化两条线的标签（Claude 仍为 5h/7d，Codex 为 Session/Weekly）。
  ### 隐私
  - 新增的 Codex 后台拉取只访问 `chatgpt.com/backend-api/wham/usage`（用本机 `codex` CLI 已登录的凭证，只读、不写回）；`history-codex.json` 仅保存两个用量百分比，不含任何 token。
---

# v0.2.8 — Codex History & Trend

## 主题

让 Codex tab 往「和 Claude tab 界面/功能一致」再走一步：给 Codex 也加**历史采样持久化** + **趋势箭头** + **额度折线图**。

- 把 v0.2.5/v0.2.3 里目前是 Claude 专属的 `UsageHistoryService`（`history.json` ring buffer）泛化成 per-provider（或新增 Codex 的等价物），让 Codex 的每次 `wham/usage` 拉取也落一个采样点。
- Codex tab 的 Session/Weekly 卡显示趋势箭头（▲▼ + 增量百分点），和 Claude 一样。
- Codex tab 加上额度折线图区块（复用 / 泛化 `UsageChartSectionView`）。
- 因为要持久化历史，Codex 这条路从「只在 popover 打开时惰性拉一次」升级到也参与一个固定 5 分钟的轻量后台采样（`CodexProvider.startPolling()`，app 启动时拉一次后每 5 分钟一次）。**`supportsBackgroundPolling` 保持 `false`** —— 该 flag 实际作用是「菜单栏 primary 候选资格」（需 provider-aware 的菜单栏渲染），本版本菜单栏渲染尚未泛化，Codex 暂不进 Settings「Primary Provider」下拉；菜单栏 provider-aware 化 + Codex 当 primary 留作后续（见 spec §6）。

**不含**：Codex 本地 session JSONL 成本扫描 / 消费热力图（→ v0.2.9）；阈值通知；多账号。

## 含 spec

- `2026-05-12-codex-history-trend` — Codex 历史采样 + 趋势箭头 + 折线图（泛化 UsageHistoryService / UsageChartSectionView）

## 验收（G6 checklist）

- [x] 所有 spec 的 spec_criteria 全 done（2026-05-12-codex-history-trend SC1~SC7）
- [x] CI 全绿（`swift build -c release`、`swift test` 217 tests、`make release-artifacts` + verify zip/dmg）—— 本机验证通过；PR CI 待跑
- [ ] CHANGELOG.md 已 append 本版本 entry（发版 runbook 时做）
- [x] 本文件 `release_notes_zh` 已填写

## 发版（G7 checklist）

- [ ] `docs/runbooks/release.md` 全流程跑通
- [ ] tag 已推送，Sparkle appcast 已更新
- [ ] GitHub Release 已创建，资源（zip/dmg）已上传
- [ ] 24h health 回访通过

## Release notes (zh)

> 从 frontmatter `release_notes_zh` 同步过来。

## 引用

- 路线：[`README.md`](./README.md)
- spec：[`../superpowers/specs/2026-05-12-codex-history-trend.md`](../superpowers/specs/2026-05-12-codex-history-trend.md)
- 前置：[`v0.2.6-codex-provider.md`](./v0.2.6-codex-provider.md)、[`v0.2.5-multi-provider-refactor.md`](./v0.2.5-multi-provider-refactor.md)
- 母法：[`../superpowers/specs/2026-05-11-docs-governance.md`](../superpowers/specs/2026-05-11-docs-governance.md)
