---
version: v0.2.5
codename: widgetkit
status: placeholder
target_date: null
shipped_date: null
includes_specs: []
release_notes_zh: |
  <to-be-filled when spec lands>
---

# v0.2.5 — widgetkit

> ⚠️ **Placeholder guardrail**：如果你正在为本版本写第一个真正的 spec，
> 请把 `status` 从 `placeholder` 升到 `planned`、清空 `includes_specs` 示例、
> 填入 `target_date`，并删除本提示框。
>
> ⚠️ **依赖 v0.2.1 公证先行**（作者评估 2026-05-11）：
> - WidgetKit extension 需要 App Group entitlement + provisioning profile + Apple Developer 证书签名 + 公证
> - 当前 SwiftPM ad-hoc 签名架构 + 无 .xcodeproj 与 widget 部署链冲突
> - PlugInKit 注册 + deep sign + 嵌入 .app/Contents/PlugIns/ 需要 build.sh 大量定制
> - **触发 §6 #1 hard gate**（Apple Developer 凭证操作）— v0.2.1 Apple 公证未落地前不可推进
> - user 已选择 v0.2.1 暂留 placeholder，故 v0.2.5 同步暂留

## 主题

WidgetKit 扩展。参考 CodexBar 的 widget 设计（砍掉 Switcher，因为我们 Claude-only）：

- **Usage widget**（small/medium/large）：当前 5h / 7d 使用率 + reset
- **History widget**（medium/large）：近 7 天使用率趋势图
- **Compact widget**（small）：today-cost / 30d-cost（依赖 v0.1.2 cost 扫描）

实施要点：
- 新加 SwiftPM target `ClaudeUsageBarWidget`
- App Group 容器写 `WidgetSnapshot` JSON，widget 读
- 注意 PlugInKit 注册、签名（特别是公证后必须 deep sign）

## 计划含的 spec

- `widgetkit`（待写）
- 可能开 ADR：App Group identifier / Bundle ID

## 调研依据

[`../research/competitive-analysis.md`](../research/competitive-analysis.md) §2.9

## 引用

- 路线：[`README.md`](./README.md)
- 母法：[`../superpowers/specs/2026-05-11-docs-governance.md`](../superpowers/specs/2026-05-11-docs-governance.md)
