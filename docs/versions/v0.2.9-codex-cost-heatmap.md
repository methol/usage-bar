---
version: v0.2.9
codename: codex-cost-heatmap
status: in-progress
target_date: 2026-05-14
shipped_date: null
includes_specs: [2026-05-12-codex-cost-heatmap]
release_notes_zh: |
  改进：
  - Codex tab 加「估算费用卡」—— 跟随时间窗口（1h/6h/1d/7d/30d）从本机 `~/.codex/sessions/**` 的 session 记录估算用量与花费（按 OpenAI list price 折算，与 Claude tab 同口径，非真实账单）。
  - Codex tab 加「消费热力图」—— 按天着色，hover 看当天 $/调用数/token；与 Claude tab 一致。
  - Codex tab 去掉「Plan: Free」卡片 —— 对齐 Claude tab。
  内部：
  - 抽出 `ModelPriceTable` 协议 + `ProviderCostContext`；新增 `OpenAIPricing` 估价表（gpt-5.x / gpt-5-codex / o-series，逐项标注「估算」+ snapshot 日期）。
  - 新增 `CodexRolloutCostParser`（状态机解析 rollout JSONL）+ `CodexUsageCollector`（扫 `~/.codex/sessions`、写独立的 `data/codex/`）；`UsageStatsService` 改 per-provider；`ScanCursorStore` per-provider（Claude 仍用旧文件名，零迁移）。
  隐私：
  - Codex session 文件含完整对话/代码 —— 只抽 token 计数 / 模型名 / 时间戳，绝不持久化或日志输出对话原文、绝对路径；落盘文件沿用 0600、目录 0700。
---

# v0.2.9 — Codex Cost & Heatmap

## 主题

Codex tab 全面对齐 Claude tab —— 加上**本地 session JSONL 成本扫描** → 30 天 USD + per-model token → **消费热力图**（和 Claude 的 `UsageHeatmapView` 一致）。

- 新增 `CodexUsageCollector`（类比 `ClaudeUsageCollector`）：扫 OpenAI codex CLI 在 `~/.codex/sessions/**` 写的 rollout JSONL，提取 token / 估算 USD，喂进 v0.2.3 的 per-provider `UsageEventStore`。
- Codex tab 加消费热力图区块（复用 / 泛化 `UsageHeatmapView` + `UsageStatsService`）。
- 至此 Codex tab = 套餐徽章 + Session/Weekly 卡（含 pace + 趋势）+ 折线图 + 消费热力图 + Updated + 底栏 —— 与 Claude tab 形态一致。

**不含**：Codex CLI RPC / chatgpt.com Web 兜底数据源；Codex 阈值通知；Codex 多账号 —— 这些按需再排（spec §6 列）。

## 含 spec

- `2026-05-12-codex-cost-heatmap` — Codex 本地 session JSONL 扫描 → 成本/token → 消费热力图

## 验收（G6 checklist）

- [x] 所有 spec 的 spec_criteria 全 done
- [ ] CI 全绿（`swift build`、`swift test`、`make release-artifacts`）— 待 PR 上 CI 跑
- [ ] CHANGELOG.md 已 append 本版本 entry — 发版 runbook 时做
- [x] 本文件 `release_notes_zh` 已填写

## 发版（G7 checklist）

- [ ] `docs/runbooks/release.md` 全流程跑通
- [ ] tag 已推送，Sparkle appcast 已更新
- [ ] GitHub Release 已创建，资源（zip/dmg）已上传
- [ ] 24h health 回访通过

## Release notes (zh)

> 从 frontmatter `release_notes_zh` 同步过来。

## 引用

- 路线：[`README.md`](./README.md)
- spec：[`../superpowers/specs/2026-05-12-codex-cost-heatmap.md`](../superpowers/specs/2026-05-12-codex-cost-heatmap.md)
- 前置：[`v0.2.8-codex-history-trend.md`](./v0.2.8-codex-history-trend.md)、[`v0.2.3-usage-store-redesign.md`](./v0.2.3-usage-store-redesign.md)、[`v0.1.2-local-cost-scan.md`](./v0.1.2-local-cost-scan.md)
- 调研：[`../research/codex-data-sources.md`](../research/codex-data-sources.md)（§4 提到 `~/.codex/sessions/**` rollout JSONL）
- 母法：[`../superpowers/specs/2026-05-11-docs-governance.md`](../superpowers/specs/2026-05-11-docs-governance.md)
