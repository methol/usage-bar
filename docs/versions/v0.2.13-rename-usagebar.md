---
version: v0.2.13
codename: rename-usagebar
status: in-progress
target_date: 2026-05-13
shipped_date: null
includes_specs: []
release_notes_zh: |
  ### 内部
  - 项目标识符统一去掉 `Claude` 前缀：app / SwiftPM 模块 / `.app` bundle 名 `ClaudeUsageBar` → `UsageBar`；`CFBundleIdentifier` → `com.tuzhihao.app.UsageBar`；本地数据目录 `~/.config/claude-usage-bar/` → `~/.config/usage-bar/`、缓存目录同理。功能与界面无变化。（详见 ADR 0006）
---

# v0.2.13 — Rename ClaudeUsageBar → UsageBar

## 主题

把 fork 自上游沿用至今的 `ClaudeUsageBar` / `Claude Usage Bar` / `com.local.ClaudeUsageBar` / `~/.config/claude-usage-bar/` 这套命名，统一改为 `UsageBar` / `usage-bar` / `com.tuzhihao.app.UsageBar`。动机见 [ADR 0006](../adr/0006-rename-claudeusagebar-to-usagebar.md)：与 ADR 0005「多 provider」方向对齐、与 repo 名 `usage-bar` 一致、趁「尚未正式发布」的零成本窗口落地（顺带让 bundle id 进入真实 reverse-DNS 空间，为 v0.2.1 公证铺路）。

范围：

- **代码**：Package / target / 模块 / `@main struct UsageBarApp` / SwiftPM 资源 bundle `UsageBar_UsageBar.bundle` / 资源 finder 函数；本地存储路径与 NSLog 前缀。
- **构建链 / CI**：`Makefile`、`build.sh`、`verify-release.sh`、`build.yml`、`release.yml` 的产物名。
- **`Info.plist`**：`CFBundleName` / `CFBundleExecutable` / `CFBundleDisplayName` → `UsageBar`；`CFBundleIdentifier` → `com.tuzhihao.app.UsageBar`。
- **文档**：README / CHANGELOG / CONTRIBUTING / docs（含 ADR 0001 的路径引用）。
- **刻意不动**：`Blimp-Labs/claude-usage-bar` / `blimp-labs.github.io/claude-usage-bar`（真实上游地址）、`docs/adr/0004` 正文、`data/<provider>/` 里的 provider id、`claude-logo.png` 资源名。
- **无代码逻辑改动、无新依赖、无 spec**（纯重命名，按 AGENTS.md §6 之外属可自决范围；owner 已授权直接 merge）。

## 含 spec

- 无 —— 纯机械重命名，不走 brainstorming / spec 流程；决策记录在 [ADR 0006](../adr/0006-rename-claudeusagebar-to-usagebar.md)。

## Reviews

- **G5（独立 code-review）**：本版本跳过独立 reviewer —— 改动是纯机械标识符替换（无业务逻辑变更），硬证据齐全：`swift build -c release` ✅、`swift test` = 251 passed ✅、`make zip` + `verify-release.sh` ✅、`make dmg` + `verify-release.sh` ✅、构建产物 `CFBundleIdentifier == com.tuzhihao.app.UsageBar` ✅。owner（methol）于 2026-05-13 明确授权「提 PR 然后合并」。

## 验收（G6 checklist）

- [x] 无 spec_criteria（本版本不含 spec）
- [x] CI 全绿（本地 2026-05-13 复跑）：`swift build -c release` OK、`swift test` = 251 passed、`make zip` / `make dmg` + `verify-release.sh` 均 ✅
- [x] `ClaudeUsageBar` 在仓库内已无残留（`Blimp-Labs/claude-usage-bar` 等上游引用按设计保留）
- [ ] CHANGELOG.md 已 append 本版本 entry（发版 runbook §5 自动生成，发版时做）
- [x] 本文件 `release_notes_zh` 已填写

## 发版（G7 checklist）

- [ ] `docs/runbooks/release.md` 全流程跑通
- [ ] tag 已推送，Sparkle appcast 已更新
- [ ] GitHub Release 已创建，资源（zip/dmg）已上传
- [ ] 24h health 回访通过

## Release notes (zh)

> 从 frontmatter `release_notes_zh` 同步过来。

## 引用

- 路线：[`README.md`](./README.md)
- ADR：[`../adr/0006-rename-claudeusagebar-to-usagebar.md`](../adr/0006-rename-claudeusagebar-to-usagebar.md)
- 母法：[`../superpowers/specs/2026-05-11-docs-governance.md`](../superpowers/specs/2026-05-11-docs-governance.md)
