---
version: v0.5.1
codename: claude-credentials-in-memory
status: in-progress
target_date: 2026-05-14
shipped_date: null
includes_specs:
  - 2026-05-14-claude-credentials-in-memory
release_notes_zh: |
  ### 移除（Removed）
  - **不再持久化 Claude 凭证到磁盘**：`~/.config/usage-bar/credentials.json` 与 `accounts.json` 不再写入；in-memory cache 在启动 / token 过期时从 Claude CLI 的 Keychain 条目重读。旧文件不会被读取也不会被覆盖，用户可自行 `rm` 清理。
  - **Claude provider 多账号 UI**：`AccountSwitcherView` 移除，`Account switcher` / `Add account` 菜单下线。请用 Claude CLI 切换当前会话；usage-bar 的"多账号"语义现在指"多 provider"（Claude / Codex / Gemini）。
  - **内置 OAuth refresh 流程**：token 刷新完全交给 Claude CLI 处理，usage-bar 不再持有 refresh_token。

  ### 修复（Fixed）
  - token 过期后 popover 卡在 "Not signed in" 状态：Retry 按钮现在真正重读 Keychain（之前因 `accounts.json` 存在被短路）。
  - 从结构上消除 issue #22 这一类 OAuth Token Rotation 失败（usage-bar 不再持有 refresh_token，CLI 轮换 token 后不会再被迫退出登录）。
---

# v0.5.1 — Claude 凭证改 in-memory only

## 主题

usage-bar 不再持久化 Claude 凭证。in-memory cache + 从 Claude CLI Keychain 重读；OAuth refresh / 多账号 UI 全部下线。

兑现 spec [`2026-05-14-claude-credentials-in-memory`](../superpowers/specs/2026-05-14-claude-credentials-in-memory.md)：以"借读 Claude CLI Keychain"的最小架构修掉 issue #22 那一类 OAuth Token Rotation 失败，并大幅收敛 Claude provider 的状态面（去 `StoredCredentialsStore` / 去 `accounts.json` / 去 OAuth client 整套）。

## 含 spec

- [`2026-05-14-claude-credentials-in-memory`](../superpowers/specs/2026-05-14-claude-credentials-in-memory.md) — Claude 凭证改 in-memory only（SC1~SC10）

## 验收（G6 checklist）

- [ ] 所有 spec 的 spec_criteria 全 done（SC1~SC10）
- [ ] CI 全绿（`swift build` + `swift test`）
- [ ] CHANGELOG.md 已 append 本版本 entry
- [x] 本文件 `release_notes_zh` 已填写

## 发版（G7 checklist）

- [ ] `docs/runbooks/release.md` 全流程跑通
- [ ] tag 已推送，Sparkle appcast 已更新
- [ ] GitHub Release 已创建，资源（zip/dmg）已上传
- [ ] 24h health 回访通过

## 升级路径

无需用户操作。首次启动新版本时：

- 若 Claude CLI Keychain 中有有效 token：30s 内 popover 自动恢复显示用量。
- 若 ACL 未授权：弹一次 Keychain 授权框，点 Always Allow 即可。
- 旧 `~/.config/usage-bar/accounts.json` + `credentials.json` 不会被读取也不会被覆盖；用户可自行 `rm` 清理。

## Release notes (zh)

> 从 frontmatter `release_notes_zh` 同步过来，见上方 frontmatter。

## 引用

- 路线：[`README.md`](./README.md)
- spec：[`../superpowers/specs/2026-05-14-claude-credentials-in-memory.md`](../superpowers/specs/2026-05-14-claude-credentials-in-memory.md)
- 关联 issue：[#22](https://github.com/methol/usage-bar/issues/22)
