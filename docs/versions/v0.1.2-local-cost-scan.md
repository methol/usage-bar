---
version: v0.1.2
codename: local-cost-scan
status: placeholder
target_date: null
shipped_date: null
includes_specs: []
release_notes_zh: |
  <to-be-filled when spec lands>
---

# v0.1.2 — local-cost-scan

> ⚠️ **Placeholder guardrail**：如果你正在为本版本写第一个真正的 spec，
> 请把 `status` 从 `placeholder` 升到 `planned`、清空 `includes_specs` 示例、
> 填入 `target_date`，并删除本提示框。

## 主题

扫本地 JSONL 日志，per-model token + 30 天 USD cost 累积。
解锁 v0.0.10 的菜单栏 `$/天` 显示模式。

实现要点（参考 CodexBar + ccusage）：

- 扫描根：`$CLAUDE_CONFIG_DIR` 列表的 `<root>/projects`，回退 `~/.config/claude/projects` 与 `~/.claude/projects`
- 解析 `type: "assistant"` 行的 `message.usage`，per-model token 计数
- 用 `message.id + requestId` 去重流式块
- 内嵌价格表（参考 [LiteLLM pricing data](https://github.com/BerriAI/litellm) 的离线快照）
- 缓存：`~/Library/Caches/claude-usage-bar/cost-usage/claude-v1.json`，60s 节流
- 结果加入 `UsageResponse`（新字段 `localCost30d`）

## 计划含的 spec

- `local-cost-scan`（待写）

## 调研依据

[`../research/competitive-analysis.md`](../research/competitive-analysis.md) §2.4 Path 4 / §5.2 Step C；ccusage 项目机制

## 引用

- 路线：[`README.md`](./README.md)
- 母法：[`../superpowers/specs/2026-05-11-docs-governance.md`](../superpowers/specs/2026-05-11-docs-governance.md)
