---
version: v0.2.14
codename: litellm-pricing
status: in-progress
target_date: 2026-05-14
shipped_date: null
includes_specs: [2026-05-13-litellm-pricing]
release_notes_zh: |
  ### 修复
  - Codex tab 的「估算费用卡」不再频繁出现「含 N 条未知模型调用（价格表过时？）」—— 价格数据改用社区维护的 LiteLLM 价格库（打包进 app、运行期每 3 小时后台自动刷新），并加了一层「模型别名 → 有价模型」的逐级回退查表，让 `gpt-5.3-codex` / `gpt-5.1-codex-max` / `gpt-5.4-mini` / `gpt-5.4-xhigh` 这类 codex CLI 内部别名也能匹配到价格。

  ### 改进
  - Claude / Codex 的「估算费用」更准，且价格随上游自动更新、不再依赖发版（仍是 list-price 估算，不是真实账单）。
  - 当定价数据暂时不可用时，费用卡会显示「定价数据未加载」而不是误导性的「价格表过时」。

  ### 内部
  - 删除手写的 `OpenAIPricing` / `ClaudePricing` 价格表；新增 `ModelPricingCatalog`（加载/解析/3h 后台刷新）；后台刷新复用 v0.2.11 起的统一 tick，不新增 Timer。打包 `THIRD_PARTY_LICENSES.txt`（LiteLLM MIT 归属）。
---

# v0.2.14 — litellm-pricing

## 主题

模型「估算费用」的价格表不再手写维护，改为打包 [LiteLLM](https://github.com/BerriAI/litellm) 的社区价格快照（`model_prices_and_context_window.json`），并在运行期每 3 小时后台刷新一次。再加一层「模型别名 → 有价模型」的逐级回退查表，让 codex CLI 的内部别名（`gpt-5.3-codex` / `gpt-5.1-codex-max` / `gpt-5.4-mini` / `gpt-5.4-xhigh` …）也能匹配到价格——修掉 Codex tab 频繁出现的「含 N 条未知模型调用（价格表过时？）」。

用户感知：Codex / Claude 估算费用更准、「未知模型」提示基本消失；价格随上游自动更新，不再依赖发版。

## 含 spec

- `2026-05-13-litellm-pricing` — 价格表改走 LiteLLM 数据源（打包快照 + 3h 后台刷新 + 逐级回退 normalize）

## Reviews

- **G2（独立 design + security review）**：approved-after-revisions（5 条 must-fix 已应用：build 后 git checkout 还原工作区、修坏 grep、持久化 fetched_at meta、OOM/解析健壮性、URL 编译期常量 + MIT 归属）。详见 spec `reviews:`。
- **G3（独立 plan-review）**：ready-with-revisions（2 must-fix + 4 should-fix 已并入 plan）。
- **G5（独立 code-review + light security）**：approved-with-nits（8 条 SC 均落地、并发模型正确无死锁、安全面齐全且未触 hard gate；should-fix S1「refresh 测试 flaky」已修为轮询等待，nit N4 已补契约注释）。
- 三轮 review 均由独立的 general-purpose subagent（跨 session、prompt 显式要求独立判断）执行 —— codex 工具不可用时按 AGENTS.md §5 fallback 表走 subagent（用户 memory 已记此偏好）。

## 验收（G6 checklist）

- [x] 所有 spec 的 spec_criteria 全 done（SC1~SC8 evidence 见 spec `## Verification log`）
- [x] CI 全绿（本地 2026-05-13 复跑）：`swift build -c release` ✅、`swift test` = 262 passed ✅、`make release-artifacts` ✅、`bash macos/scripts/verify-release.sh macos/UsageBar.{zip,dmg}` ✅
- [ ] CHANGELOG.md 已 append 本版本 entry（发版 runbook §5 自动生成，发版时做）
- [x] 本文件 `release_notes_zh` 已填写

## 发版（G7 checklist）

- [ ] `docs/runbooks/release.md` 全流程跑通
- [ ] tag 已推送，Sparkle appcast 已更新
- [ ] GitHub Release 已创建，资源（zip/dmg）已上传
- [ ] 24h health 回访通过

## Release notes (zh)

> 从 frontmatter `release_notes_zh` 同步过来。

## 引用

- 路线：[`README.md`](./README.md)
- spec：[`../superpowers/specs/2026-05-13-litellm-pricing.md`](../superpowers/specs/2026-05-13-litellm-pricing.md)
- 母法：[`../superpowers/specs/2026-05-11-docs-governance.md`](../superpowers/specs/2026-05-11-docs-governance.md)
