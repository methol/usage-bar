---
version: v0.2.4
codename: cli-pty-fallback
status: placeholder
target_date: null
shipped_date: null
includes_specs: []
release_notes_zh: |
  <to-be-filled when spec lands>
---

# v0.2.4 — cli-pty-fallback

> ⚠️ **Placeholder guardrail**：如果你正在为本版本写第一个真正的 spec，
> 请把 `status` 从 `placeholder` 升到 `planned`、清空 `includes_specs` 示例、
> 填入 `target_date`，并删除本提示框。

## 主题

`claude` CLI PTY 兜底路径：当 OAuth / cookie 都不可用时，启动 PTY 跑 `claude --allowed-tools ""` 并发送 `/usage` / `/status`，解析渲染输出。

实施参考 CodexBar Claude provider Path 3：
- `ClaudeCLISession` PTY 控制器（参考 CodexBar `ClaudeCLISession.swift`）
- 自动应答首启 prompts（trust files / workspace / telemetry）
- 解析 ANSI 后定位 "Current session" / "Current week" header
- 可选守护进程 `ClaudeUsageBarWatchdog` 维持稳定 PTY（参考 `CodexBarClaudeWatchdog`）

## 计划含的 spec

- `cli-pty-fallback`（待写）

## 调研依据

[`../research/competitive-analysis.md`](../research/competitive-analysis.md) §2.4 Path 3

## 引用

- 路线：[`README.md`](./README.md)
- 母法：[`../superpowers/specs/2026-05-11-docs-governance.md`](../superpowers/specs/2026-05-11-docs-governance.md)
