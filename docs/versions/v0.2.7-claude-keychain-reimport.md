---
version: v0.2.7
codename: claude-keychain-reimport
status: in-progress
target_date: 2026-05-12
shipped_date: null
includes_specs: [2026-05-12-claude-keychain-reimport]
release_notes_zh: |
  ### 修复
  - **不再误报「Session expired」**：当菜单栏 app 自己的 Claude 凭证过期、但你本机的 `claude` CLI 还在正常登录状态时，app 会自动从 `claude` 的钥匙串凭证续上，不再弹「Session expired — please sign in again」逼你重新登录。仅在单账号时生效；只读你的钥匙串，不会创建或改动它；过期凭证后台续读不弹授权框。
---

# v0.2.7 — Claude Keychain Re-import

## 主题

修一个早就存在的体验坑：菜单栏 app 用的是它自己的 OAuth 凭证（`~/.config/claude-usage-bar/credentials.json`），跟 Claude Code 用的 Keychain 凭证（`Claude Code-credentials`）是两套。app 那套 token 过期、且 refresh token 也失效时，直接弹「Session expired — please sign in again」逼用户重登 —— 而此时 Claude Code 自己往往还在正常用（Keychain 里有新鲜 token）。

本版本：**当 Claude 的 refresh 失败时，先回退去读一次 Claude CLI Keychain（`ClaudeCLICredentialsStrategy`）；若那里有可用凭证就静默续上、不打扰用户；只有 Keychain 也没有时才走原来的「session 过期、请重新登录」路径。** 复用现成的 `bootstrapFromCLIIfNeeded` 同一套 Keychain 读取逻辑，不引入新依赖、不改凭证存储格式。

## 含 spec

- `2026-05-12-claude-keychain-reimport` — refresh 失败 → 回退读 Claude CLI Keychain → 续上凭证

## 验收（G6 checklist）

- [x] 所有 spec 的 spec_criteria 全 done（SC1–SC5）
- [x] CI 全绿（`swift build -c release`、`swift test` = 207 passed、`make release-artifacts` + verify-release zip/dmg OK）
- [ ] CHANGELOG.md 已 append 本版本 entry（发版 runbook §5 自动生成，merge 后做）
- [x] 本文件 `release_notes_zh` 已填写

## 发版（G7 checklist）

- [ ] `docs/runbooks/release.md` 全流程跑通
- [ ] tag 已推送，Sparkle appcast 已更新
- [ ] GitHub Release 已创建，资源（zip/dmg）已上传
- [ ] 24h health 回访通过

## Release notes (zh)

> 从 frontmatter `release_notes_zh` 同步过来。

## 引用

- 路线：[`README.md`](./README.md)
- spec：[`../superpowers/specs/2026-05-12-claude-keychain-reimport.md`](../superpowers/specs/2026-05-12-claude-keychain-reimport.md)
- 相关 spec：[`../superpowers/specs/2026-05-11-claude-cli-credentials.md`](../superpowers/specs/2026-05-11-claude-cli-credentials.md)（v0.1.1 的 Keychain bootstrap）
- 母法：[`../superpowers/specs/2026-05-11-docs-governance.md`](../superpowers/specs/2026-05-11-docs-governance.md)
